import TicketUploadForm from "@/components/forms/TicketUploadForm";
import React from "react";

const page = () => {
  return (
    <div>
      <TicketUploadForm />
    </div>
  );
};

export default page;
