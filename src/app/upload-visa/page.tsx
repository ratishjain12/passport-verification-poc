import VisaUploadForm from "@/components/forms/VisaUploadForm";
import React from "react";

const page = () => {
  return (
    <div>
      <VisaUploadForm />
    </div>
  );
};

export default page;
